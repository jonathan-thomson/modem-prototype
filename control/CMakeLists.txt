# Zephyr application for the Control MCU firmware
cmake_minimum_required(VERSION 3.20.0)

find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(control)

# App sources
# (Unit libraries will be added later with add_subdirectory(src/<unit>) etc.)
target_sources(app PRIVATE
  src/main.c
)
